\chapter{AI Hardware Architecture and Compiler Constraints}
\label{chap:ch03}
\typeout{START_CHAPTER "ch03" \theabspage}

This chapter maps hardware constraints that every later compiler pass must encode \citep{patel2025llminference,chen2020compilerbasedhardw,yirage2026}. The mistake to avoid is optimizing prefill FLOPs while batch-1 decode misses p99 SLOs \citep{dao2023flashdecoding,kwon2023vllm}. Problem-first, hardware-bound, multi-target---same bar as Chapter~1 \citep{dlbook,semianalysis2024tma,amd2024aie}.

TaxBreak dense Llama-3.2-1B averages 847.5 kernel launches per output token on H100; OLMoE-1B/7B averages 9,305 \citep{taxbreak2026}. Memory-Floor CUDA Graphs on Qwen-2.5-7B (BS${=}1$, ctx${=}2048$) yields $1.259\times$ on H100 by removing host overhead---yet cannot fix illegal HBM traffic \citep{memoryfloor2026,nvidia2024cudagraphs}.

\section{hardware landscape}
\label{sec:hardware_landscape}

% AUTO_VISUAL:tab_hw_landscape

\begin{table}[t]
\centering
\caption{AI accelerator classes and primary execution models.}
\label{tab:hw_landscape}
\begin{tabular}{lcc}
\toprule
\visTableHeader
\textbf{Class} & \textbf{Execution model} & \textbf{Decode fit} \\
\midrule
NVIDIA GPU & SIMT + tensor cores & Graphs, fusion \\
AMD GPU & Wavefront + matrix cores & HIP fusion \\
x86/ARM CPU & Cache + SIMD & Coarse fusion \\
Edge NPU & Spatial dataflow & Static MegaKernel \\
\bottomrule
\end{tabular}
\end{table}

Teams hit \textbf{hardware landscape} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because NVIDIA or AMD or x86 or arm or NPU was planned without NVIDIA or AMD or x86 or arm or NPU residency on the SKU you ship \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound NVIDIA or AMD or x86 or arm or NPU with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on NVIDIA or AMD or x86 or arm or NPU/NVIDIA or AMD or x86 or arm or NPU legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize NVIDIA or AMD or x86 or arm or NPU with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NVIDIA or AMD or x86 or arm or NPU---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused NVIDIA or AMD or x86 or arm or NPU raises bytes/token; fused schedule must keep NVIDIA or AMD or x86 or arm or NPU on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds NVIDIA, AMD, x86, arm, NPU to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{constraint matrix}
\label{sec:constraint_matrix}

% AUTO_VISUAL:tab_constraint_matrix

\begin{table}[t]
\centering
\caption{Cross-hardware constraint matrix.}
\label{tab:constraint_matrix}
\begin{tabular}{lccc}
\toprule
\visTableHeader
\textbf{Dimension} & \textbf{GPU} & \textbf{CPU} & \textbf{NPU} \\
\midrule
On-chip SRAM & Shared mem / L1 & L1/L2/L3 & Tile buffers \\
Parallel grain & Warp & Core + SIMD & Spatial PE array \\
Dynamic shapes & Flexible & Flexible & Restricted \\
\bottomrule
\end{tabular}
\end{table}

The production surprise at \textbf{constraint matrix} is not peak FLOPs; it is illegal tier placement: bandwidth or memory hierarchy or parallel spilling to HBM/DDR while bandwidth or memory hierarchy or parallel still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: bandwidth or memory hierarchy or parallel producers must feed bandwidth or memory hierarchy or parallel consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for bandwidth or memory hierarchy or parallel on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds bandwidth, memory hierarchy, parallel to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{GPU constraints}
\label{sec:gpu_constraints}

% AUTO_VISUAL:fig_gpu_memory_hierarchy

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Register file\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize Shared memory\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize L2 cache\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize HBM\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{GPU memory hierarchy constraints for decode megakernels.}
\label{fig:gpu_memory_hierarchy}
\end{figure}

\textbf{GPU constraints} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix warp or shared memory or tensor core and warp or shared memory or tensor core together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound warp or shared memory or tensor core with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on warp or shared memory or tensor core/warp or shared memory or tensor core legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize warp or shared memory or tensor core with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked warp or shared memory or tensor core---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused warp or shared memory or tensor core raises bytes/token; fused schedule must keep warp or shared memory or tensor core on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds warp, shared memory, tensor core to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{CPU constraints}
\label{sec:ch03_cpu_constraints}

Teams hit \textbf{CPU constraints} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because cache or SIMD or NUMA was planned without cache or SIMD or NUMA residency on the SKU you ship \citep{nvidia2022hopper,amd2024aie}.

Trace residency: cache or SIMD or NUMA producers must feed cache or SIMD or NUMA consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for cache or SIMD or NUMA on target SKU before merge \citep{memoryfloor2026,yirage2026}. Spec hint: CPU Cache SIMD/AVX NUMA.

\paragraph{Scope.} This section binds cache, SIMD, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{NPU constraints}
\label{sec:ch03_npu_constraints}

The production surprise at \textbf{NPU constraints} is not peak FLOPs; it is illegal tier placement: static or NPU or edge spilling to HBM/DDR while static or NPU or edge still pays launch or DMA tax every token \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound static or NPU or edge with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static or NPU or edge/static or NPU or edge legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static or NPU or edge with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked static or NPU or edge---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static or NPU or edge raises bytes/token; fused schedule must keep static or NPU or edge on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, NPU, edge to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{hardware aware compile}
\label{sec:ch03_hardware_aware_compile}

\textbf{hardware aware compile} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix tiling or layout or pass and tiling or layout or pass together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: tiling or layout or pass producers must feed tiling or layout or pass consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for tiling or layout or pass on target SKU before merge \citep{memoryfloor2026,yirage2026}. Spec hint: Tiling.

\paragraph{Scope.} This section binds tiling, layout, pass to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{benchmark method}
\label{sec:benchmark_method}

% AUTO_VISUAL:fig_benchmark_method

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.9, transform shape]
 \node[vis pipeline node] (s0) at (-3.40,0) {\footnotesize Fixed model+ctx\\Repro config};
 \node[vis arch node] (s1) at (0.00,0) {\footnotesize Normalize metrics\\bytes/token};
 \node[vis pipeline node] (s2) at (3.40,0) {\footnotesize Compare targets\\GPU / CPU / NPU};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
\end{tikzpicture}
\caption{Cross-hardware benchmark methodology used in this book.}
\label{fig:benchmark_method}
\end{figure}

Teams hit \textbf{benchmark method} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because benchmark or metric or methodology was planned without benchmark or metric or methodology residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound benchmark or metric or methodology with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on benchmark or metric or methodology/benchmark or metric or methodology legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize benchmark or metric or methodology with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked benchmark or metric or methodology---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused benchmark or metric or methodology raises bytes/token; fused schedule must keep benchmark or metric or methodology on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds benchmark, metric, methodology to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{YiRage modeling}
\label{sec:ch03_yirage_modeling}

The production surprise at \textbf{YiRage modeling} is not peak FLOPs; it is illegal tier placement: YiRage or chip arch or model spilling to HBM/DDR while YiRage or chip arch or model still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage or chip arch or model producers must feed YiRage or chip arch or model consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage or chip arch or model on target SKU before merge \citep{memoryfloor2026,yirage2026}. Spec hint: YiRage ChipArchitecture.

\paragraph{Scope.} This section binds YiRage, chip  arch, model to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\textbf{hardware landscape} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix NVIDIA or AMD or x86 or arm or NPU and NVIDIA or AMD or x86 or arm or NPU together or bytes/token flatlines while kernel microbenchs celebrate \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound NVIDIA or AMD or x86 or arm or NPU with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on NVIDIA or AMD or x86 or arm or NPU/NVIDIA or AMD or x86 or arm or NPU legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize NVIDIA or AMD or x86 or arm or NPU with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NVIDIA or AMD or x86 or arm or NPU---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused NVIDIA or AMD or x86 or arm or NPU raises bytes/token; fused schedule must keep NVIDIA or AMD or x86 or arm or NPU on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds NVIDIA, AMD, x86, arm, NPU to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{constraint matrix} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because bandwidth or memory hierarchy or parallel was planned without bandwidth or memory hierarchy or parallel residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: bandwidth or memory hierarchy or parallel producers must feed bandwidth or memory hierarchy or parallel consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for bandwidth or memory hierarchy or parallel on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds bandwidth, memory hierarchy, parallel to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{GPU constraints} is not peak FLOPs; it is illegal tier placement: warp or shared memory or tensor core spilling to HBM/DDR while warp or shared memory or tensor core still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound warp or shared memory or tensor core with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on warp or shared memory or tensor core/warp or shared memory or tensor core legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize warp or shared memory or tensor core with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked warp or shared memory or tensor core---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused warp or shared memory or tensor core raises bytes/token; fused schedule must keep warp or shared memory or tensor core on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds warp, shared memory, tensor core to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{CPU constraints} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix cache or SIMD or NUMA and cache or SIMD or NUMA together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,amd2024aie}.

Trace residency: cache or SIMD or NUMA producers must feed cache or SIMD or NUMA consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for cache or SIMD or NUMA on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds cache, SIMD, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{NPU constraints} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because static or NPU or edge was planned without static or NPU or edge residency on the SKU you ship \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound static or NPU or edge with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static or NPU or edge/static or NPU or edge legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static or NPU or edge with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked static or NPU or edge---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static or NPU or edge raises bytes/token; fused schedule must keep static or NPU or edge on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, NPU, edge to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{hardware aware compile} is not peak FLOPs; it is illegal tier placement: tiling or layout or pass spilling to HBM/DDR while tiling or layout or pass still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: tiling or layout or pass producers must feed tiling or layout or pass consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for tiling or layout or pass on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds tiling, layout, pass to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{benchmark method} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix benchmark or metric or methodology and benchmark or metric or methodology together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound benchmark or metric or methodology with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on benchmark or metric or methodology/benchmark or metric or methodology legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize benchmark or metric or methodology with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked benchmark or metric or methodology---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused benchmark or metric or methodology raises bytes/token; fused schedule must keep benchmark or metric or methodology on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds benchmark, metric, methodology to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage modeling} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because YiRage or chip arch or model was planned without YiRage or chip arch or model residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage or chip arch or model producers must feed YiRage or chip arch or model consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage or chip arch or model on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, chip  arch, model to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{hardware landscape} is not peak FLOPs; it is illegal tier placement: NVIDIA or AMD or x86 or arm or NPU spilling to HBM/DDR while NVIDIA or AMD or x86 or arm or NPU still pays launch or DMA tax every token \citep{amd2024xdna,amd2024aie}.

Trace residency: NVIDIA or AMD or x86 or arm or NPU producers must feed NVIDIA or AMD or x86 or arm or NPU consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for NVIDIA or AMD or x86 or arm or NPU on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds NVIDIA, AMD, x86, arm, NPU to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{constraint matrix} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix bandwidth or memory hierarchy or parallel and bandwidth or memory hierarchy or parallel together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound bandwidth or memory hierarchy or parallel with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on bandwidth or memory hierarchy or parallel/bandwidth or memory hierarchy or parallel legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize bandwidth or memory hierarchy or parallel with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked bandwidth or memory hierarchy or parallel---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused bandwidth or memory hierarchy or parallel raises bytes/token; fused schedule must keep bandwidth or memory hierarchy or parallel on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds bandwidth, memory hierarchy, parallel to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{GPU constraints} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because warp or shared memory or tensor core was planned without warp or shared memory or tensor core residency on the SKU you ship \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: warp or shared memory or tensor core producers must feed warp or shared memory or tensor core consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for warp or shared memory or tensor core on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds warp, shared memory, tensor core to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{CPU constraints} is not peak FLOPs; it is illegal tier placement: cache or SIMD or NUMA spilling to HBM/DDR while cache or SIMD or NUMA still pays launch or DMA tax every token \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound cache or SIMD or NUMA with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on cache or SIMD or NUMA/cache or SIMD or NUMA legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize cache or SIMD or NUMA with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cache or SIMD or NUMA---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused cache or SIMD or NUMA raises bytes/token; fused schedule must keep cache or SIMD or NUMA on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds cache, SIMD, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{NPU constraints} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix static or NPU or edge and static or NPU or edge together or bytes/token flatlines while kernel microbenchs celebrate \citep{amd2024xdna,amd2024aie}.

Trace residency: static or NPU or edge producers must feed static or NPU or edge consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for static or NPU or edge on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds static, NPU, edge to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{hardware aware compile} when AI Hardware Architecture and Compiler Constraints looks correct in isolation yet decode telemetry disagrees---usually because tiling or layout or pass was planned without tiling or layout or pass residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound tiling or layout or pass with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on tiling or layout or pass/tiling or layout or pass legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize tiling or layout or pass with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked tiling or layout or pass---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused tiling or layout or pass raises bytes/token; fused schedule must keep tiling or layout or pass on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds tiling, layout, pass to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{benchmark method} is not peak FLOPs; it is illegal tier placement: benchmark or metric or methodology spilling to HBM/DDR while benchmark or metric or methodology still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: benchmark or metric or methodology producers must feed benchmark or metric or methodology consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for benchmark or metric or methodology on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds benchmark, metric, methodology to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage modeling} is where AI Hardware Architecture and Compiler Constraints meets Chapter~1 counters: fix YiRage or chip arch or model and YiRage or chip arch or model together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage or chip arch or model with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage or chip arch or model/YiRage or chip arch or model legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage or chip arch or model with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked YiRage or chip arch or model---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage or chip arch or model raises bytes/token; fused schedule must keep YiRage or chip arch or model on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, chip  arch, model to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\section{Key Takeaways}
\label{sec:ch03_key_takeaways}

\begin{itemize}
\item \textbf{Hardware Landscape.} Tie hardware landscape to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Constraint Matrix.} Tie constraint matrix to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{GPU Constraints.} Tie GPU constraints to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{CPU Constraints.} Tie CPU constraints to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{NPU Constraints.} Tie NPU constraints to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Measure useful decode work.} Prefer kernels/token and bytes/token over peak FLOPs \citep{patel2025llminference,dlbook}.
\item \textbf{Mechanical sympathy.} Co-design schedules with on-chip residency \citep{dao2022flashattention,chen2020compilerbasedhardw}.
\end{itemize}

\section{Conclusion}
\label{sec:ch03_conclusion}

AI Hardware Architecture and Compiler Constraints is a residency contract under decode metrics Chapter~1 established \citep{taxbreak2026,memoryfloor2026}. Later chapters assume AI Hardware Architecture and Compiler Constraints baselines when fusing decoder schedules. Gate merges on buffer accounting, not microbench FLOPs alone \citep{yirage2026,semianalysis2024tma,amd2024aie}.

\typeout{END_CHAPTER "ch03" \theabspage}
