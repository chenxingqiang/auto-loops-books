\chapter{TMA Double-Buffer Pipelines and Async Movement}
\label{chap:ch08}
\typeout{START_CHAPTER "ch08" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{memoryfloor2026}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026}.

\section{Pipeline Theory}
\label{sec:pipeline_theory}
% AUTO_VISUAL:fig_pipeline_theory_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Pipeline Theory};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Pipeline Theory optimization pipeline in TMA Double-Buffer Pipelines and Async Movement.}
\label{fig:pipeline_theory_pipeline}
\end{figure}


Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{zhang2020learninghowtodrive}. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{shukla2020doubledifferential}. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{zhao2023rbgcrepurposethebu}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm}.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{li2021trajectorybasedpri}. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference}. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie}.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2020async2020commentar}. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper}. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook}.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2020async2020committee}. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2023flashdecoding}. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{GPU TMA}
\label{sec:gpu_tma}
% AUTO_VISUAL:fig_gpu_tma_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{GPU TMA block diagram for TMA Double-Buffer Pipelines and Async Movement.}
\label{fig:gpu_tma_architecture}
\end{figure}


GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2023async2023coverpage}. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2020async2020index}. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2020freshideaspapersas}. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2020async2020toc}. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{CPU Dma}
\label{sec:cpu_dma}

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2023async2023organizin}. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2018messagefromtheasyn}. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement \citep{anon2023async2023messagefr}. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{NPU Pipeline}
\label{sec:npu_pipeline}

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{YiRage Pipeline Gen}
\label{sec:yirage_pipeline_gen}

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{yirage2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Chapter Summary}
\label{sec:ch08_summary}

The through-line for ch08 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist.


\subsection{Engineering Deep Dive}
\label{sec:ch08_deep_dive}

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Pipeline Theory is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{pipeline}, \textbf{double-buffer} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds pipeline double-buffer to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU TMA is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{TMA}, \textbf{Hopper} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds TMA Hopper to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Dma is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{dma}, \textbf{CPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dma CPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Pipeline is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{NPU}, \textbf{dma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU dma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Pipeline Gen is a production bottleneck surface for TMA Double-Buffer Pipelines and Async Movement. Teams that treat \textbf{YiRage}, \textbf{pipeline} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage pipeline to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\typeout{END_CHAPTER "ch08" \theabspage}
