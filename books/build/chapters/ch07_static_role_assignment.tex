\chapter{Static Role Assignment and Schedule-Free Kernels}
\label{chap:ch07}
\typeout{START_CHAPTER "ch07" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{memoryfloor2026}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026}.

\section{Static Roles}
\label{sec:static_roles}
% AUTO_VISUAL:fig_static_roles_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Static Roles};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Static Roles optimization pipeline in Static Role Assignment and Schedule-Free Kernels.}
\label{fig:static_roles_pipeline}
\end{figure}


Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels \citep{babati2023theroleofimplement}. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{tan2019rolebasedstaticdes}. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3}.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels \citep{masri2021optimalpublicelect}. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference}. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie}.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels \citep{laguardia2022universityclasssch}. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2023flashdecoding}. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook}.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels \citep{david2018linearlypricedtime}. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Thread Mapping}
\label{sec:thread_mapping}
% AUTO_VISUAL:fig_thread_mapping_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Thread Mapping block diagram for Static Role Assignment and Schedule-Free Kernels.}
\label{fig:thread_mapping_architecture}
\end{figure}


Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{No Runtime Sched}
\label{sec:no_runtime_sched}

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{YiRage Tiling}
\label{sec:yirage_tiling}

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{yirage2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Chapter Summary}
\label{sec:ch07_summary}

The through-line for ch07 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist.


\subsection{Engineering Deep Dive}
\label{sec:ch07_deep_dive}

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Roles is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{static}, \textbf{role} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static role to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Thread Mapping is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{thread}, \textbf{warp} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds thread warp to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

No Runtime Sched is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{runtime}, \textbf{dispatch} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds runtime dispatch to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Tiling is a production bottleneck surface for Static Role Assignment and Schedule-Free Kernels. Teams that treat \textbf{YiRage}, \textbf{tiling} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage tiling to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\typeout{END_CHAPTER "ch07" \theabspage}
