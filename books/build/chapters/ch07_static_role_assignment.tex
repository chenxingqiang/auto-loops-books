\chapter{Static Role Assignment and Schedule-Free Kernels}
\label{chap:ch07}
\typeout{START_CHAPTER "ch07" \theabspage}

Parts~I--II established decode metrics and hardware contracts; \emph{Static Role Assignment and Schedule-Free Kernels} shows how those contracts become concrete schedules on silicon \citep{nvidia2022hopper,amd2024xdna,dao2022flashattention}. The mistake to avoid is optimizing prefill FLOPs while batch-1 decode misses p99 SLOs \citep{dao2023flashdecoding,kwon2023vllm}. Problem-first, hardware-bound, multi-target---same bar as Chapter~1 \citep{dlbook,semianalysis2024tma,amd2024aie}.

TaxBreak dense Llama-3.2-1B averages 847.5 kernel launches per output token on H100; OLMoE-1B/7B averages 9,305 \citep{taxbreak2026}. Memory-Floor CUDA Graphs on Qwen-2.5-7B (BS${=}1$, ctx${=}2048$) yields $1.259\times$ on H100 by removing host overhead---yet cannot fix illegal HBM traffic \citep{memoryfloor2026,nvidia2024cudagraphs}.

\section{static roles}
\label{sec:ch07_static_roles}

% AUTO_VISUAL:fig_static_roles_pipeline

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Static Roles};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Static Roles optimization pipeline in Static Role Assignment and Schedule-Free Kernels.}
\label{fig:static_roles_pipeline}
\end{figure}

Teams hit \textbf{static roles} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because static was planned without role residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound static with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static/role legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked role---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static raises bytes/token; fused schedule must keep role on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{thread mapping}
\label{sec:ch07_thread_mapping}

% AUTO_VISUAL:fig_thread_mapping_architecture

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Thread Mapping block diagram for Static Role Assignment and Schedule-Free Kernels.}
\label{fig:thread_mapping_architecture}
\end{figure}

The production surprise at \textbf{thread mapping} is not peak FLOPs; it is illegal tier placement: thread spilling to HBM/DDR while warp still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: thread producers must feed warp consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for thread on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{no runtime sched}
\label{sec:ch07_no_runtime_sched}

\textbf{no runtime sched} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix runtime and dispatch together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/dispatch legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked dispatch---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep dispatch on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{YiRage tiling}
\label{sec:ch07_yirage_tiling}

Teams hit \textbf{YiRage tiling} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without tiling residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed tiling consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\textbf{static roles} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix static and role together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound static with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static/role legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked role---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static raises bytes/token; fused schedule must keep role on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{thread mapping} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because thread was planned without warp residency on the SKU you ship \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: thread producers must feed warp consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for thread on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{no runtime sched} is not peak FLOPs; it is illegal tier placement: runtime spilling to HBM/DDR while dispatch still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/dispatch legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked dispatch---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep dispatch on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage tiling} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix YiRage and tiling together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed tiling consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{static roles} is not peak FLOPs; it is illegal tier placement: static spilling to HBM/DDR while role still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: static producers must feed role consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for static on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{thread mapping} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix thread and warp together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound thread with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on thread/warp legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize thread with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked warp---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused thread raises bytes/token; fused schedule must keep warp on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{no runtime sched} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because runtime was planned without dispatch residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: runtime producers must feed dispatch consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for runtime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{YiRage tiling} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while tiling still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/tiling legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked tiling---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep tiling on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{static roles} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because static was planned without role residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound static with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static/role legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked role---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static raises bytes/token; fused schedule must keep role on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{thread mapping} is not peak FLOPs; it is illegal tier placement: thread spilling to HBM/DDR while warp still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: thread producers must feed warp consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for thread on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{no runtime sched} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix runtime and dispatch together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/dispatch legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked dispatch---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep dispatch on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage tiling} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without tiling residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed tiling consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{static roles} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix static and role together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Trace residency: static producers must feed role consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for static on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{thread mapping} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because thread was planned without warp residency on the SKU you ship \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound thread with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on thread/warp legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize thread with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked warp---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused thread raises bytes/token; fused schedule must keep warp on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{no runtime sched} is not peak FLOPs; it is illegal tier placement: runtime spilling to HBM/DDR while dispatch still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: runtime producers must feed dispatch consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for runtime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage tiling} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix YiRage and tiling together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/tiling legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked tiling---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep tiling on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{static roles} is not peak FLOPs; it is illegal tier placement: static spilling to HBM/DDR while role still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound static with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on static/role legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize static with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked role---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused static raises bytes/token; fused schedule must keep role on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{thread mapping} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix thread and warp together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: thread producers must feed warp consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for thread on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{no runtime sched} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because runtime was planned without dispatch residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/dispatch legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked dispatch---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep dispatch on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{YiRage tiling} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while tiling still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed tiling consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{static roles} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because static was planned without role residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: static producers must feed role consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for static on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds static, role to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{thread mapping} is not peak FLOPs; it is illegal tier placement: thread spilling to HBM/DDR while warp still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound thread with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on thread/warp legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize thread with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked warp---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused thread raises bytes/token; fused schedule must keep warp on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds thread, warp to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{no runtime sched} is where Static Role Assignment and Schedule-Free Kernels meets Chapter~1 counters: fix runtime and dispatch together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Trace residency: runtime producers must feed dispatch consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for runtime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds runtime, dispatch to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage tiling} when Static Role Assignment and Schedule-Free Kernels looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without tiling residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/tiling legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked tiling---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep tiling on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, tiling to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\section{Key Takeaways}
\label{sec:ch07_key_takeaways}

\begin{itemize}
\item \textbf{Static Roles.} Tie static roles to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Thread Mapping.} Tie thread mapping to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{No Runtime Sched.} Tie no runtime sched to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{YiRage Tiling.} Tie YiRage tiling to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Measure useful decode work.} Prefer kernels/token and bytes/token over peak FLOPs \citep{patel2025llminference,dlbook}.
\item \textbf{Mechanical sympathy.} Co-design schedules with on-chip residency \citep{dao2022flashattention,chen2020compilerbasedhardw}.
\end{itemize}

\section{Conclusion}
\label{sec:ch07_conclusion}

Static Role Assignment and Schedule-Free Kernels is a residency contract under decode metrics Chapter~1 established \citep{taxbreak2026,memoryfloor2026}. Later chapters assume Static Role Assignment and Schedule-Free Kernels baselines when fusing decoder schedules. Gate merges on buffer accounting, not microbench FLOPs alone \citep{yirage2026,semianalysis2024tma,amd2024aie}.

\typeout{END_CHAPTER "ch07" \theabspage}
