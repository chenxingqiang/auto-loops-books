\chapter{Heterogeneous Compilation and Multi-Hardware Deployment}
\label{chap:ch24}
\typeout{START_CHAPTER "ch24" \theabspage}

TaxBreak dense Llama-3.2-1B averages 847.5 kernel launches per output token on H100; OLMoE-1B/7B averages 9,305 \citep{taxbreak2026}. Memory-Floor CUDA Graphs on Qwen-2.5-7B (BS${=}1$, ctx${=}2048$) yields $1.259\times$ on H100 by removing host overhead---yet cannot fix illegal HBM traffic \citep{memoryfloor2026,nvidia2024cudagraphs}.

\section{hetero cluster}
\label{sec:ch24_hetero_cluster}

% AUTO_VISUAL:fig_hetero_cluster_pipeline

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Hetero Cluster};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Hetero Cluster optimization pipeline in Heterogeneous Compilation and Multi-Hardware Deployment.}
\label{fig:hetero_cluster_pipeline}
\end{figure}

Teams hit \textbf{hetero cluster} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because hetero was planned without cluster residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound hetero with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on hetero/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize hetero with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused hetero raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{unified stack}
\label{sec:ch24_unified_stack}

% AUTO_VISUAL:fig_unified_stack_architecture

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Unified Stack block diagram for Heterogeneous Compilation and Multi-Hardware Deployment.}
\label{fig:unified_stack_architecture}
\end{figure}

The production surprise at \textbf{unified stack} is not peak FLOPs; it is illegal tier placement: MLIR spilling to HBM/DDR while unified still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: MLIR producers must feed unified consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for MLIR on target SKU before merge \citep{memoryfloor2026,yirage2026}. Spec hint: MLIR/IR GPU/CPU/NPU.

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{runtime adapt}
\label{sec:ch24_runtime_adapt}

\textbf{runtime adapt} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix runtime and detect together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/detect legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked detect---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep detect on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{version regression}
\label{sec:ch24_version_regression}

Teams hit \textbf{version regression} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because version was planned without regression residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: version producers must feed regression consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for version on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{industrial cases}
\label{sec:ch24_industrial_cases}

The production surprise at \textbf{industrial cases} is not peak FLOPs; it is illegal tier placement: case spilling to HBM/DDR while production still pays launch or DMA tax every token \citep{patel2025llminference,memoryfloor2026}.

Hardware truth: GPUs bound case with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on case/production legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize case with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked production---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused case raises bytes/token; fused schedule must keep production on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{YiRage cluster}
\label{sec:ch24_yirage_cluster}

\textbf{YiRage cluster} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix YiRage and cluster together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. Spec hint: YiRage ClusterTopology.

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\textbf{hetero cluster} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix hetero and cluster together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound hetero with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on hetero/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize hetero with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused hetero raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{unified stack} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because MLIR was planned without unified residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: MLIR producers must feed unified consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for MLIR on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{runtime adapt} is not peak FLOPs; it is illegal tier placement: runtime spilling to HBM/DDR while detect still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/detect legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked detect---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep detect on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{version regression} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix version and regression together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Trace residency: version producers must feed regression consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for version on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{industrial cases} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because case was planned without production residency on the SKU you ship \citep{patel2025llminference,memoryfloor2026}.

Hardware truth: GPUs bound case with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on case/production legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize case with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked production---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused case raises bytes/token; fused schedule must keep production on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{YiRage cluster} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while cluster still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{hetero cluster} is not peak FLOPs; it is illegal tier placement: hetero spilling to HBM/DDR while cluster still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: hetero producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for hetero on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{unified stack} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix MLIR and unified together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound MLIR with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on MLIR/unified legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize MLIR with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked unified---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused MLIR raises bytes/token; fused schedule must keep unified on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{runtime adapt} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because runtime was planned without detect residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: runtime producers must feed detect consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for runtime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{version regression} is not peak FLOPs; it is illegal tier placement: version spilling to HBM/DDR while regression still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound version with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on version/regression legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize version with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked regression---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused version raises bytes/token; fused schedule must keep regression on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{industrial cases} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix case and production together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,memoryfloor2026}.

Trace residency: case producers must feed production consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for case on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage cluster} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without cluster residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{hetero cluster} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because hetero was planned without cluster residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound hetero with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on hetero/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize hetero with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused hetero raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{unified stack} is not peak FLOPs; it is illegal tier placement: MLIR spilling to HBM/DDR while unified still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: MLIR producers must feed unified consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for MLIR on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{runtime adapt} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix runtime and detect together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/detect legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked detect---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep detect on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{version regression} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because version was planned without regression residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Trace residency: version producers must feed regression consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for version on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{industrial cases} is not peak FLOPs; it is illegal tier placement: case spilling to HBM/DDR while production still pays launch or DMA tax every token \citep{patel2025llminference,memoryfloor2026}.

Hardware truth: GPUs bound case with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on case/production legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize case with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked production---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused case raises bytes/token; fused schedule must keep production on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage cluster} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix YiRage and cluster together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{hetero cluster} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix hetero and cluster together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Trace residency: hetero producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for hetero on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{unified stack} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because MLIR was planned without unified residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound MLIR with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on MLIR/unified legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize MLIR with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked unified---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused MLIR raises bytes/token; fused schedule must keep unified on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{runtime adapt} is not peak FLOPs; it is illegal tier placement: runtime spilling to HBM/DDR while detect still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: runtime producers must feed detect consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for runtime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{version regression} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix version and regression together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound version with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on version/regression legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize version with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked regression---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused version raises bytes/token; fused schedule must keep regression on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{industrial cases} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because case was planned without production residency on the SKU you ship \citep{patel2025llminference,memoryfloor2026}.

Trace residency: case producers must feed production consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for case on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{YiRage cluster} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while cluster still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{hetero cluster} is not peak FLOPs; it is illegal tier placement: hetero spilling to HBM/DDR while cluster still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound hetero with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on hetero/cluster legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize hetero with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked cluster---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused hetero raises bytes/token; fused schedule must keep cluster on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds hetero, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{unified stack} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix MLIR and unified together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: MLIR producers must feed unified consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for MLIR on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds MLIR, unified to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{runtime adapt} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because runtime was planned without detect residency on the SKU you ship \citep{patel2025llminference,dlbook}.

Hardware truth: GPUs bound runtime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on runtime/detect legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize runtime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked detect---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused runtime raises bytes/token; fused schedule must keep detect on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds runtime, detect to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{version regression} is not peak FLOPs; it is illegal tier placement: version spilling to HBM/DDR while regression still pays launch or DMA tax every token \citep{patel2025llminference,dlbook}.

Trace residency: version producers must feed regression consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for version on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds version, regression to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{industrial cases} is where Heterogeneous Compilation and Multi-Hardware Deployment meets Chapter~1 counters: fix case and production together or bytes/token flatlines while kernel microbenchs celebrate \citep{patel2025llminference,memoryfloor2026}.

Hardware truth: GPUs bound case with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on case/production legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize case with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked production---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused case raises bytes/token; fused schedule must keep production on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds case, production to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage cluster} when Heterogeneous Compilation and Multi-Hardware Deployment looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without cluster residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed cluster consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, cluster to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\section{Key Takeaways}
\label{sec:ch24_key_takeaways}

\begin{itemize}
\item \textbf{Hetero Cluster.} Tie hetero cluster to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Unified Stack.} Tie unified stack to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Runtime Adapt.} Tie runtime adapt to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Version Regression.} Tie version regression to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Industrial Cases.} Tie industrial cases to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Measure useful decode work.} Prefer kernels/token and bytes/token over peak FLOPs \citep{patel2025llminference,dlbook}.
\item \textbf{Mechanical sympathy.} Co-design schedules with on-chip residency \citep{dao2022flashattention,chen2020compilerbasedhardw}.
\end{itemize}

\section{Conclusion}
\label{sec:ch24_conclusion}

Heterogeneous Compilation and Multi-Hardware Deployment is a residency contract under decode metrics Chapter~1 established \citep{taxbreak2026,memoryfloor2026}. Later chapters assume Heterogeneous Compilation and Multi-Hardware Deployment baselines when fusing decoder schedules. Gate merges on buffer accounting, not microbench FLOPs alone \citep{yirage2026,semianalysis2024tma,amd2024aie}.

\typeout{END_CHAPTER "ch24" \theabspage}
