\chapter{Data Residency and On-Chip Memory Design}
\label{chap:ch06}
\typeout{START_CHAPTER "ch06" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{jyoti2019expressionempowere}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026}.

\section{Lifetime Planning}
\label{sec:lifetime_planning}
% AUTO_VISUAL:fig_lifetime_planning_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Lifetime Planning};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Lifetime Planning optimization pipeline in Data Residency and On-Chip Memory Design.}
\label{fig:lifetime_planning_pipeline}
\end{figure}


Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{bera2026machinelearningdri}. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{byeon2024multithreadeddatas}. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2023flashdecoding}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs}.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{chen2023astandardcellmemor}. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{seo2023advancesandtrendso}. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{GPU Residency}
\label{sec:gpu_residency}
% AUTO_VISUAL:fig_gpu_residency_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{GPU Residency block diagram for Data Residency and On-Chip Memory Design.}
\label{fig:gpu_residency_architecture}
\end{figure}


GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{purkayastha2021designstudyonimpac}. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{lai2018onchipmemoryoptimi}. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{liu2020anovelschemetomapc}. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{agarwal2024lowpowerestimation}. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{CPU Residency}
\label{sec:cpu_residency}

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{wu2022designofsystemonch}. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{dao2022flashattention}. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{kwon2023vllm}. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design \citep{addisie2020centaurhybridproce}. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{NPU Residency}
\label{sec:npu_residency}

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{YiRage Memory Pass}
\label{sec:yirage_memory_pass}

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026}. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{yirage2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Chapter Summary}
\label{sec:ch06_summary}

The through-line for ch06 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist.


\subsection{Engineering Deep Dive}
\label{sec:ch06_deep_dive}

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

GPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{NUMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds cache NUMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

NPU Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{NPU}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds NPU sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{YiRage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\typeout{END_CHAPTER "ch06" \theabspage}
