\chapter{Data Residency and On-Chip Memory Design}
\label{chap:ch06}
\typeout{START_CHAPTER "ch06" \theabspage}

Parts~I--II established decode metrics and hardware contracts; \emph{Data Residency and On-Chip Memory Design} shows how those contracts become concrete schedules on silicon \citep{nvidia2022hopper,amd2024xdna,dao2022flashattention}. The mistake to avoid is optimizing prefill FLOPs while batch-1 decode misses p99 SLOs \citep{dao2023flashdecoding,kwon2023vllm}. Problem-first, hardware-bound, multi-target---same bar as Chapter~1 \citep{dlbook,semianalysis2024tma,amd2024aie}.

TaxBreak dense Llama-3.2-1B averages 847.5 kernel launches per output token on H100; OLMoE-1B/7B averages 9,305 \citep{taxbreak2026}. Memory-Floor CUDA Graphs on Qwen-2.5-7B (BS${=}1$, ctx${=}2048$) yields $1.259\times$ on H100 by removing host overhead---yet cannot fix illegal HBM traffic \citep{memoryfloor2026,nvidia2024cudagraphs}.

\section{lifetime planning}
\label{sec:ch06_lifetime_planning}

% AUTO_VISUAL:fig_lifetime_planning_pipeline

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Lifetime Planning};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Lifetime Planning optimization pipeline in Data Residency and On-Chip Memory Design.}
\label{fig:lifetime_planning_pipeline}
\end{figure}

Teams hit \textbf{lifetime planning} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because lifetime was planned without residen residency on the SKU you ship \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound lifetime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on lifetime/residen legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize lifetime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked residen---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused lifetime raises bytes/token; fused schedule must keep residen on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{GPU residency}
\label{sec:ch06_gpu_residency}

% AUTO_VISUAL:fig_gpu_residency_architecture

\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{GPU Residency block diagram for Data Residency and On-Chip Memory Design.}
\label{fig:gpu_residency_architecture}
\end{figure}

The production surprise at \textbf{GPU residency} is not peak FLOPs; it is illegal tier placement: shared spilling to HBM/DDR while register still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: shared producers must feed register consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for shared on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{CPU residency}
\label{sec:ch06_cpu_residency}

\textbf{CPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix cache and NUMA together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound cache with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on cache/NUMA legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize cache with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NUMA---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused cache raises bytes/token; fused schedule must keep NUMA on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{NPU residency}
\label{sec:ch06_npu_residency}

Teams hit \textbf{NPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because NPU was planned without sram residency on the SKU you ship \citep{amd2024xdna,amd2024aie}.

Trace residency: NPU producers must feed sram consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for NPU on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\section{YiRage memory pass}
\label{sec:ch06_yirage_memory_pass}

The production surprise at \textbf{YiRage memory pass} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while memory still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/memory legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked memory---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep memory on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.

\textbf{lifetime planning} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix lifetime and residen together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound lifetime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on lifetime/residen legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize lifetime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked residen---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused lifetime raises bytes/token; fused schedule must keep residen on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{GPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because shared was planned without register residency on the SKU you ship \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: shared producers must feed register consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for shared on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{CPU residency} is not peak FLOPs; it is illegal tier placement: cache spilling to HBM/DDR while NUMA still pays launch or DMA tax every token \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound cache with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on cache/NUMA legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize cache with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NUMA---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused cache raises bytes/token; fused schedule must keep NUMA on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{NPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix NPU and sram together or bytes/token flatlines while kernel microbenchs celebrate \citep{amd2024xdna,amd2024aie}.

Trace residency: NPU producers must feed sram consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for NPU on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage memory pass} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without memory residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/memory legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked memory---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep memory on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{lifetime planning} is not peak FLOPs; it is illegal tier placement: lifetime spilling to HBM/DDR while residen still pays launch or DMA tax every token \citep{nvidia2022hopper,amd2024aie}.

Trace residency: lifetime producers must feed residen consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for lifetime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{GPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix shared and register together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound shared with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on shared/register legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize shared with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked register---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused shared raises bytes/token; fused schedule must keep register on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{CPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because cache was planned without NUMA residency on the SKU you ship \citep{nvidia2022hopper,amd2024aie}.

Trace residency: cache producers must feed NUMA consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for cache on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{NPU residency} is not peak FLOPs; it is illegal tier placement: NPU spilling to HBM/DDR while sram still pays launch or DMA tax every token \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound NPU with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on NPU/sram legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize NPU with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked sram---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused NPU raises bytes/token; fused schedule must keep sram on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage memory pass} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix YiRage and memory together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed memory consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{lifetime planning} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because lifetime was planned without residen residency on the SKU you ship \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound lifetime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on lifetime/residen legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize lifetime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked residen---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused lifetime raises bytes/token; fused schedule must keep residen on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{GPU residency} is not peak FLOPs; it is illegal tier placement: shared spilling to HBM/DDR while register still pays launch or DMA tax every token \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: shared producers must feed register consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for shared on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{CPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix cache and NUMA together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound cache with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on cache/NUMA legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize cache with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NUMA---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused cache raises bytes/token; fused schedule must keep NUMA on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{NPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because NPU was planned without sram residency on the SKU you ship \citep{amd2024xdna,amd2024aie}.

Trace residency: NPU producers must feed sram consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for NPU on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{YiRage memory pass} is not peak FLOPs; it is illegal tier placement: YiRage spilling to HBM/DDR while memory still pays launch or DMA tax every token \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/memory legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked memory---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep memory on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{lifetime planning} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix lifetime and residen together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,amd2024aie}.

Trace residency: lifetime producers must feed residen consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for lifetime on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{GPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because shared was planned without register residency on the SKU you ship \citep{nvidia2022hopper,semianalysis2024tma}.

Hardware truth: GPUs bound shared with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on shared/register legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize shared with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked register---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused shared raises bytes/token; fused schedule must keep register on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{CPU residency} is not peak FLOPs; it is illegal tier placement: cache spilling to HBM/DDR while NUMA still pays launch or DMA tax every token \citep{nvidia2022hopper,amd2024aie}.

Trace residency: cache producers must feed NUMA consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for cache on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{NPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix NPU and sram together or bytes/token flatlines while kernel microbenchs celebrate \citep{amd2024xdna,amd2024aie}.

Hardware truth: GPUs bound NPU with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on NPU/sram legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize NPU with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked sram---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused NPU raises bytes/token; fused schedule must keep sram on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{YiRage memory pass} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because YiRage was planned without memory residency on the SKU you ship \citep{yirage2026,chen2020compilerbasedhardw}.

Trace residency: YiRage producers must feed memory consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for YiRage on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{lifetime planning} is not peak FLOPs; it is illegal tier placement: lifetime spilling to HBM/DDR while residen still pays launch or DMA tax every token \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound lifetime with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on lifetime/residen legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize lifetime with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked residen---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused lifetime raises bytes/token; fused schedule must keep residen on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds lifetime, residen to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{GPU residency} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix shared and register together or bytes/token flatlines while kernel microbenchs celebrate \citep{nvidia2022hopper,semianalysis2024tma}.

Trace residency: shared producers must feed register consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for shared on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds shared, register to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



Teams hit \textbf{CPU residency} when Data Residency and On-Chip Memory Design looks correct in isolation yet decode telemetry disagrees---usually because cache was planned without NUMA residency on the SKU you ship \citep{nvidia2022hopper,amd2024aie}.

Hardware truth: GPUs bound cache with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on cache/NUMA legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize cache with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked NUMA---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused cache raises bytes/token; fused schedule must keep NUMA on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds cache, NUMA to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



The production surprise at \textbf{NPU residency} is not peak FLOPs; it is illegal tier placement: NPU spilling to HBM/DDR while sram still pays launch or DMA tax every token \citep{amd2024xdna,amd2024aie}.

Trace residency: NPU producers must feed sram consumers without a DDR round trip \citep{dao2022flashattention,yirage2026}. Silent spill reads as ``mysterious'' p99 regression \citep{patel2025llminference,semianalysis2024tma}.

Operational fix: diff IR or CUDA for new allocs; count launches/token (GPU) or DMA edges (NPU); require graph capture legality on GPU and static legality on XDNA \citep{nvidia2024cudagraphs,amd2024aie}.

\paragraph{Review gate.} Attach roofline or buffer accounting for NPU on target SKU before merge \citep{memoryfloor2026,yirage2026}. 

\paragraph{Scope.} This section binds NPU, sram to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\textbf{YiRage memory pass} is where Data Residency and On-Chip Memory Design meets Chapter~1 counters: fix YiRage and memory together or bytes/token flatlines while kernel microbenchs celebrate \citep{yirage2026,chen2020compilerbasedhardw}.

Hardware truth: GPUs bound YiRage with shared memory/TMA; CPUs with cache/NUMA; NPUs with static tile SRAM and DMA chains \citep{nvidia2022hopper,amd2024xdna,dlbook}. Decode at batch-1 stays bandwidth- and orchestration-limited---thin matmuls rarely dominate \citep{memoryfloor2026,taxbreak2026}.

Compiler/kernel fix: predicate fusion on YiRage/memory legality before codegen; fork passes per target \citep{chen2020compilerbasedhardw,yirage2026}. YiRage encodes iron rules from Chapter~2 as checkable IR metadata \citep{kwon2023vllm,liu2024deepseekv3}.

\paragraph{Multi-hardware delta.} Hopper may legalize YiRage with TMA double-buffer; XDNA requires static tile assignment; CPU needs cache-blocked memory---identical math, different bytes/token \citep{semianalysis2024tma,amd2024xdna,dlbook}.

\begin{example}
Decode cell (BS${=}1$, ctx${=}2048$): unfused YiRage raises bytes/token; fused schedule must keep memory on-chip across GPU, CPU, and NPU baselines \citep{taxbreak2026,memoryfloor2026,yirage2026}.
\end{example}

\paragraph{Scope.} This section binds YiRage, memory to decode residency and orchestration metrics on GPU, CPU, and NPU targets \citep{chen2020compilerbasedhardw,yirage2026,patel2025llminference}.



\section{Key Takeaways}
\label{sec:ch06_key_takeaways}

\begin{itemize}
\item \textbf{Lifetime Planning.} Tie lifetime planning to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{GPU Residency.} Tie GPU residency to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{CPU Residency.} Tie CPU residency to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{NPU Residency.} Tie NPU residency to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{YiRage Memory Pass.} Tie YiRage memory pass to bytes/token and multi-hardware legality \citep{yirage2026,taxbreak2026,memoryfloor2026}.
\item \textbf{Measure useful decode work.} Prefer kernels/token and bytes/token over peak FLOPs \citep{patel2025llminference,dlbook}.
\item \textbf{Mechanical sympathy.} Co-design schedules with on-chip residency \citep{dao2022flashattention,chen2020compilerbasedhardw}.
\end{itemize}

\section{Conclusion}
\label{sec:ch06_conclusion}

Data Residency and On-Chip Memory Design is a residency contract under decode metrics Chapter~1 established \citep{taxbreak2026,memoryfloor2026}. Later chapters assume Data Residency and On-Chip Memory Design baselines when fusing decoder schedules. Gate merges on buffer accounting, not microbench FLOPs alone \citep{yirage2026,semianalysis2024tma,amd2024aie}.

\typeout{END_CHAPTER "ch06" \theabspage}
