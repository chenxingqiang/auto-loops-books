\chapter{AMD XDNA/AIE Dataflow Architecture}
\label{chap:ch05}
\typeout{START_CHAPTER "ch05" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{patel2025llminference}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{steffen20254thgenamdcdnasuptm}.

\section{XDNA Tiles}
\label{sec:xdna_tiles}
% AUTO_VISUAL:fig_xdna_tiles_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\XDNA Tiles};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{XDNA Tiles optimization pipeline in AMD XDNA/AIE Dataflow Architecture.}
\label{fig:xdna_tiles_pipeline}
\end{figure}


XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{delaye2022cgra4hpc2022invite}. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{heinz2024tapascoaieanopenso}. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{rico2024amdxdnanpuinryzena}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention}.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{vilim2021aurochsanarchitect}. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2023flashdecoding}. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{alves2023distributedcheckpo}. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{bouaziz2025benchmarkingfloati}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{weng2020ahybridsystolicdat}. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Static Dataflow Rules}
\label{sec:static_dataflow_rules}
% AUTO_VISUAL:fig_static_dataflow_rules_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Static Dataflow Rules block diagram for AMD XDNA/AIE Dataflow Architecture.}
\label{fig:static_dataflow_rules_architecture}
\end{figure}


Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{krichene2024adataflowarchitect}. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{nvidia2022hopper}. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{smith2024amdinstinctmi300xg}. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture \citep{nvidia2024cudagraphs}. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{CUDA XDNA Mapping}
\label{sec:cuda_xdna_mapping}

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Online Softmax HW}
\label{sec:online_softmax_hw}

Online Softmax HW is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Architecture Tradeoffs}
\label{sec:architecture_tradeoffs}

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{CUDA}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds CUDA XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Dataflow Conclusion}
\label{sec:dataflow_conclusion}

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026}. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{yirage2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{YiRage XDNA Backend}
\label{sec:yirage_xdna_backend}

YiRage XDNA Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{YiRage}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Chapter Summary}
\label{sec:ch05_summary}

The through-line for ch05 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist.


\subsection{Engineering Deep Dive}
\label{sec:ch05_deep_dive}

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Online Softmax HW is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{CUDA}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds CUDA XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage XDNA Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{YiRage}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Online Softmax HW is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{CUDA}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds CUDA XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage XDNA Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{YiRage}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Online Softmax HW is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{CUDA}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds CUDA XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage XDNA Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{YiRage}, \textbf{XDNA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage XDNA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

XDNA Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{XDNA}, \textbf{AIE}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds XDNA AIE tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

CUDA XDNA Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{TMA} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds buffer descriptor TMA to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Online Softmax HW is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\typeout{END_CHAPTER "ch05" \theabspage}
