\chapter{Hierarchical Synchronization and Communication}
\label{chap:ch09}
\typeout{START_CHAPTER "ch09" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{patel2025llminference}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026}.

\section{Sync Hierarchy}
\label{sec:sync_hierarchy}
% AUTO_VISUAL:fig_sync_hierarchy_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Sync Hierarchy};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
 \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\MegaKernel stage};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Sync Hierarchy optimization pipeline in Hierarchical Synchronization and Communication.}
\label{fig:sync_hierarchy_pipeline}
\end{figure}


Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{mahgoli2026ahierarchicalaidri}. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{hu2021plisyncprefetchlos}. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{sviridov2018tosyncornottosyncw}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm}.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{hunold2018hierarchicalclocks}. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nakada2024lipsyncmlmachinele}. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna}.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{klimov2018featuresofsynchron}. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026}. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs}.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{wiesner2024timesynchronizatio}. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2023flashdecoding}. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Hardware Sync Pick}
\label{sec:hardware_sync_pick}
% AUTO_VISUAL:fig_hardware_sync_pick_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
 \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
 \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
 \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
 \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
 \draw[vis arrow] (s0.east) -- (s1.west);
 \draw[vis arrow] (s1.east) -- (s2.west);
 \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Hardware Sync Pick block diagram for Hierarchical Synchronization and Communication.}
\label{fig:hardware_sync_pick_architecture}
\end{figure}


Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{anon20212021ieeeinternatio}. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{anon20242024ieeeinternatio}. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{mkacher2018securetimesynchron}. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{wiesner2025timesynchronizatio}. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Deadlock Detection}
\label{sec:deadlock_detection}

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{coleman2021multicoreintraproc}. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication \citep{grunichev2018analysisoftimeforh}. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{YiRage Sync Pass}
\label{sec:yirage_sync_pass}

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{yirage2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\section{Chapter Summary}
\label{sec:ch09_summary}

The through-line for ch09 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist.


\subsection{Engineering Deep Dive}
\label{sec:ch09_deep_dive}

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Hardware Sync Pick is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{GPU}, \textbf{CPU}, \textbf{NPU} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds GPU CPU NPU to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Deadlock Detection is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{deadlock} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds deadlock to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

YiRage Sync Pass is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{YiRage}, \textbf{sync} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds YiRage sync to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

Sync Hierarchy is a production bottleneck surface for Hierarchical Synchronization and Communication. Teams that treat \textbf{sync}, \textbf{barrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets. The hardware--software contract here binds sync barrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags.

\typeout{END_CHAPTER "ch09" \theabspage}
