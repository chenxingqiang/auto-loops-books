# ch30 expand to 5000w (Loop R30)

- Added `Co-design decode benchmarking methodology` section + counter table (`tab:ch30_codesign_benchmark`).
- Pre-Key-Takeaways paragraphs: boundary replay, manifest pinning, triplet backend matrix, mode map interaction, regression bisect, org metrics, canary/rollback, long-ctx, speculative decode, golden triple CI, disaggregated pools, postmortem schema, nightly cadence, worksheet archive.
- 4100 -> 5000 words under book-wide min_words gate.

## Verify
python3 book_prepare.py --chapter ch30
