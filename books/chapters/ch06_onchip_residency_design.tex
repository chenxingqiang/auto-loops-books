\chapter{Data Residency and On-Chip Memory Design}
\label{chap:ch06}
\typeout{START_CHAPTER "ch06" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{patel2025llminference,nvidia2022hopper}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026,semianalysis2024tma}.

\section{Lifetime Planning}
\label{sec:lifetime_planning}
% AUTO_VISUAL:fig_lifetime_planning_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
    \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Lifetime Planning};
    \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
    \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\Megakernel stage};
    \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
    \draw[vis arrow] (s0.east) -- (s1.west);
    \draw[vis arrow] (s1.east) -- (s2.west);
    \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Lifetime Planning optimization pipeline in Data Residency and On-Chip Memory Design.}
\label{fig:lifetime_planning_pipeline}
\end{figure}


Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

\section{Gpu Residency}
\label{sec:gpu_residency}
% AUTO_VISUAL:fig_gpu_residency_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
    \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
    \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
    \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
    \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
    \draw[vis arrow] (s0.east) -- (s1.west);
    \draw[vis arrow] (s1.east) -- (s2.west);
    \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Gpu Residency block diagram for Data Residency and On-Chip Memory Design.}
\label{fig:gpu_residency_architecture}
\end{figure}


Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

\section{Cpu Residency}
\label{sec:cpu_residency}

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

\section{Npu Residency}
\label{sec:npu_residency}

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

\section{Yirage Memory Pass}
\label{sec:yirage_memory_pass}

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

\section{Chapter Summary}
\label{sec:ch06_summary}

The through-line for ch06 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics \citep{memoryfloor2026,amd2024xdna}. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist \citep{semianalysis2024tma,dlbook}.


\subsection{Engineering Deep Dive}
\label{sec:ch06_deep_dive}

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook,nvidia2024cudagraphs}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention,liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm,patel2025llminference}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs,taxbreak2026}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2022flashattention,liu2024deepseekv3}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm,patel2025llminference}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs,taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3,memoryfloor2026}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{kwon2023vllm,patel2025llminference}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2024cudagraphs,taxbreak2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3,memoryfloor2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{patel2025llminference,nvidia2022hopper}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook,nvidia2024cudagraphs}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention,liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm,patel2025llminference}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs,taxbreak2026}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2022flashattention,liu2024deepseekv3}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm,patel2025llminference}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs,taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3,memoryfloor2026}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{kwon2023vllm,patel2025llminference}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2024cudagraphs,taxbreak2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3,memoryfloor2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{patel2025llminference,nvidia2022hopper}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Lifetime Planning is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{lifetime}, \textbf{residen} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds lifetime residen to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Gpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{shared}, \textbf{register} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds shared register to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Cpu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{cache}, \textbf{numa} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook,nvidia2024cudagraphs}. The hardware--software contract here binds cache numa to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention,liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm,patel2025llminference}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs,taxbreak2026}.

Npu Residency is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{npu}, \textbf{sram} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2022flashattention,liu2024deepseekv3}. The hardware--software contract here binds npu sram to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm,patel2025llminference}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs,taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3,memoryfloor2026}.

Yirage Memory Pass is a production bottleneck surface for Data Residency and On-Chip Memory Design. Teams that treat \textbf{yirage}, \textbf{memory} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{kwon2023vllm,patel2025llminference}. The hardware--software contract here binds yirage memory to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2024cudagraphs,taxbreak2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3,memoryfloor2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{patel2025llminference,nvidia2022hopper}.

\typeout{END_CHAPTER "ch06" \theabspage}
