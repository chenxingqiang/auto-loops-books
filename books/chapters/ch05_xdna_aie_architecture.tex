\chapter{AMD XDNA/AIE Dataflow Architecture}
\label{chap:ch05}
\typeout{START_CHAPTER "ch05" \theabspage}

Production decode teams rarely fail for lack of framework features---they fail because compiler and kernel schedules violate residency, launch, and bandwidth contracts on the SKU they actually ship \citep{patel2025llminference,nvidia2022hopper}. This chapter continues the book's problem-first thread: name the contradiction, trace it to hardware limits, then show how passes and megakernels recover SLO on GPU, CPU, and NPU fleets \citep{taxbreak2026,semianalysis2024tma}.

\section{Xdna Tiles}
\label{sec:xdna_tiles}
% AUTO_VISUAL:fig_xdna_tiles_pipeline
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
    \node[vis pipeline node] (s0) at (-5.03,0) {\footnotesize Input\\Xdna Tiles};
    \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip\\Tiling / residency};
    \node[vis pipeline node] (s2) at (1.67,0) {\footnotesize Fused compute\\Megakernel stage};
    \node[vis arch node] (s3) at (5.03,0) {\footnotesize Output\\Next layer / KV};
    \draw[vis arrow] (s0.east) -- (s1.west);
    \draw[vis arrow] (s1.east) -- (s2.west);
    \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Xdna Tiles optimization pipeline in AMD XDNA/AIE Dataflow Architecture.}
\label{fig:xdna_tiles_pipeline}
\end{figure}


Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

\section{Static Dataflow Rules}
\label{sec:static_dataflow_rules}
% AUTO_VISUAL:fig_static_dataflow_rules_architecture
\begin{figure}[t]
\centering
\begin{tikzpicture}[vis base, scale=0.88, transform shape]
    \node[vis arch node] (s0) at (-5.03,0) {\footnotesize Host\\};
    \node[vis arch node] (s1) at (-1.68,0) {\footnotesize On-chip buffer\\};
    \node[vis arch node] (s2) at (1.67,0) {\footnotesize Compute array\\};
    \node[vis arch node] (s3) at (5.03,0) {\footnotesize Memory tier\\};
    \draw[vis arrow] (s0.east) -- (s1.west);
    \draw[vis arrow] (s1.east) -- (s2.west);
    \draw[vis arrow] (s2.east) -- (s3.west);
\end{tikzpicture}
\caption{Static Dataflow Rules block diagram for AMD XDNA/AIE Dataflow Architecture.}
\label{fig:static_dataflow_rules_architecture}
\end{figure}


Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

\section{Cuda Xdna Mapping}
\label{sec:cuda_xdna_mapping}

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

\section{Online Softmax Hw}
\label{sec:online_softmax_hw}

Online Softmax Hw is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

\section{Architecture Tradeoffs}
\label{sec:architecture_tradeoffs}

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{cuda}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds cuda xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

\section{Dataflow Conclusion}
\label{sec:dataflow_conclusion}

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

\section{Yirage Xdna Backend}
\label{sec:yirage_xdna_backend}

Yirage Xdna Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{yirage}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds yirage xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

\section{Chapter Summary}
\label{sec:ch05_summary}

The through-line for ch05 is unchanged: hardware constraints dictate compiler passes, fusion boundaries, and deployment metrics \citep{memoryfloor2026,amd2024xdna}. Carry bytes/token and launches/token spreadsheets forward; later chapters assume those baselines exist \citep{semianalysis2024tma,dlbook}.


\subsection{Engineering Deep Dive}
\label{sec:ch05_deep_dive}

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Online Softmax Hw is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook,nvidia2024cudagraphs}. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention,liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm,patel2025llminference}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs,taxbreak2026}.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{cuda}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2022flashattention,liu2024deepseekv3}. The hardware--software contract here binds cuda xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm,patel2025llminference}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs,taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3,memoryfloor2026}.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{kwon2023vllm,patel2025llminference}. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2024cudagraphs,taxbreak2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3,memoryfloor2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{patel2025llminference,nvidia2022hopper}.

Yirage Xdna Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{yirage}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds yirage xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Online Softmax Hw is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{cuda}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds cuda xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

Yirage Xdna Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{yirage}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024xdna,dao2022flashattention}. The hardware--software contract here binds yirage xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024aie,kwon2023vllm}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dlbook,nvidia2024cudagraphs}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dao2022flashattention,liu2024deepseekv3}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{amd2024aie,kwon2023vllm}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dlbook,nvidia2024cudagraphs}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{dao2022flashattention,liu2024deepseekv3}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{kwon2023vllm,patel2025llminference}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dlbook,nvidia2024cudagraphs}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{dao2022flashattention,liu2024deepseekv3}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{kwon2023vllm,patel2025llminference}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2024cudagraphs,taxbreak2026}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{dao2022flashattention,liu2024deepseekv3}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{kwon2023vllm,patel2025llminference}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2024cudagraphs,taxbreak2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{liu2024deepseekv3,memoryfloor2026}.

Online Softmax Hw is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{kwon2023vllm,patel2025llminference}. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2024cudagraphs,taxbreak2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{liu2024deepseekv3,memoryfloor2026}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{patel2025llminference,nvidia2022hopper}.

Architecture Tradeoffs is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{cuda}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2024cudagraphs,taxbreak2026}. The hardware--software contract here binds cuda xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{liu2024deepseekv3,memoryfloor2026}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{patel2025llminference,nvidia2022hopper}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{taxbreak2026,semianalysis2024tma}.

Dataflow Conclusion is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{dataflow}, \textbf{inference} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{liu2024deepseekv3,memoryfloor2026}. The hardware--software contract here binds dataflow inference to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{patel2025llminference,nvidia2022hopper}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{taxbreak2026,semianalysis2024tma}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{memoryfloor2026,amd2024xdna}.

Yirage Xdna Backend is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{yirage}, \textbf{xdna} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{patel2025llminference,nvidia2022hopper}. The hardware--software contract here binds yirage xdna to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{taxbreak2026,semianalysis2024tma}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{memoryfloor2026,amd2024xdna}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{nvidia2022hopper,amd2024aie}.

Xdna Tiles is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{xdna}, \textbf{aie}, \textbf{tile} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{taxbreak2026,semianalysis2024tma}. The hardware--software contract here binds xdna aie tile to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{memoryfloor2026,amd2024xdna}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{nvidia2022hopper,amd2024aie}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{semianalysis2024tma,dlbook}.

Static Dataflow Rules is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{static}, \textbf{dataflow} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{memoryfloor2026,amd2024xdna}. The hardware--software contract here binds static dataflow to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{nvidia2022hopper,amd2024aie}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{semianalysis2024tma,dlbook}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024xdna,dao2022flashattention}.

Cuda Xdna Mapping is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{buffer descriptor}, \textbf{tma} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{nvidia2022hopper,amd2024aie}. The hardware--software contract here binds buffer descriptor tma to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{semianalysis2024tma,dlbook}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024xdna,dao2022flashattention}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{amd2024aie,kwon2023vllm}.

Online Softmax Hw is a production bottleneck surface for AMD XDNA/AIE Dataflow Architecture. Teams that treat \textbf{softmax}, \textbf{carrier} as independent knobs routinely ship kernels that win microbenchmarks yet miss decode SLOs on real fleets \citep{semianalysis2024tma,dlbook}. The hardware--software contract here binds softmax carrier to measurable bytes/token and launches/token outcomes across GPU, CPU, and NPU targets \citep{amd2024xdna,dao2022flashattention}. YiRage lowers the same constraints as explicit legality checks before codegen, so illegal fusion fails in CI rather than in customer dashboards \citep{amd2024aie,kwon2023vllm}. Industrial readers should map each paragraph to the Chapter~3 constraint matrix and record fleet measurements alongside compiler flags \citep{dlbook,nvidia2024cudagraphs}.

\typeout{END_CHAPTER "ch05" \theabspage}
